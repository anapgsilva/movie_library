<%= link_to "Add New Director", new_director_path %>

<h1>Movie Directors</h1>

<p>Found <%= pluralize @directors.uniq.count, "director" %>.</p>


<% @directors.uniq.each do |director| %>
  <h3><%=  link_to director.name, director %></h3>
<% end %>
